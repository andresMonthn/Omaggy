/** Define los tipos base de CMS soportados por el proyecto. */
// we can add more types here if we have more CMSs
// ex. export type CmsType = 'contentlayer' | 'other-cms';
export type CmsType = 'wordpress' | 'keystatic';
